<%= simple_form_for(@audio) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
  </div>
  <%= f.input :file, as: :file %>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
